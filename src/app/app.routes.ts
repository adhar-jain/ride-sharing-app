import { Routes } from '@angular/router';
import { TestPageComponent } from './test-page/test-page.component'; // or your actual component

export const routes: Routes = [
  { path: 'test', component: TestPageComponent },
];
